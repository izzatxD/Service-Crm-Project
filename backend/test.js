const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ include: { staffProfile: true } }); console.log(users.map(u => u.email + ' - ' + (u.staffProfile ? u.staffProfile.primaryRole : 'no role'))); } main();
